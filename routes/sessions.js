// Routes pour la gestion des sessions (simplifiées)
const express = require('express');
const router = express.Router();

// Pas de routes publiques pour les sessions
// La gestion se fait automatiquement via le contrôleur d'authentification

module.exports = router;
