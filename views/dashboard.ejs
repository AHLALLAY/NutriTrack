    <!-- Contenu principal -->
    <div class="min-h-screen px-4 sm:px-6 lg:px-8">
        <div class="max-w-5xl mx-auto">
            <!-- En-tête -->
            <div class="mb-8">
                <h1 class="text-2xl sm:text-3xl font-bold text-gray-900">Bonjour <span class="text-nutri-green"><%= utilisateur ? utilisateur.nomComplet : 'Utilisateur' %></span> 👋</h1>
                <p class="text-sm text-gray-500">Voici votre résumé nutritionnel du jour</p>
            </div>

            <!-- KPIs -->
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-6">
                <!-- Carte Calories -->
                <div class="bg-white rounded-[16px] border border-[#E2E8F0] p-4">
                    <div class="flex items-center justify-between mb-2">
                        <span class="text-sm text-gray-600">Calories</span>
                        <i class="fas fa-fire text-orange-500"></i>
                    </div>
                    <div class="text-2xl font-bold text-gray-900">
                        <%= kpis.calories.valeur %>/<%= kpis.calories.objectif %>
                    </div>
                    <div class="text-xs text-gray-500 mt-1"><%= kpis.calories.pourcentage %>% de l'objectif</div>
                    <div class="w-full h-2 bg-gray-200 rounded mt-3">
                        <% var caloriesWidth = Math.min(kpis.calories.pourcentage, 100); %>
                        <div class="h-2 bg-green-500 rounded" data-width="<%= caloriesWidth %>"></div>
                    </div>
                </div>

                <!-- Carte Hydratation -->
                <div class="bg-white rounded-[16px] border border-[#E2E8F0] p-4">
                    <div class="flex items-center justify-between mb-2">
                        <span class="text-sm text-gray-600">Hydratation</span>
                        <i class="fas fa-tint text-blue-500"></i>
                    </div>
                    <div class="text-2xl font-bold text-gray-900">
                        <%= kpis.hydratation.valeurLitres %>L / <%= kpis.hydratation.objectifLitres %>L
                    </div>
                    <div class="text-xs text-gray-500 mt-1"><%= kpis.hydratation.pourcentage %>% de l'objectif</div>
                    <div class="w-full h-2 bg-gray-200 rounded mt-3">
                        <% var hydratationWidth = Math.min(kpis.hydratation.pourcentage, 100); %>
                        <div class="h-2 bg-blue-500 rounded" data-width="<%= hydratationWidth %>"></div>
                    </div>
                </div>

                <!-- Carte Protéines -->
                <div class="bg-white rounded-[16px] border border-[#E2E8F0] p-4">
                    <div class="flex items-center justify-between mb-2">
                        <span class="text-sm text-gray-600">Protéines</span>
                        <i class="fas fa-dumbbell text-green-500"></i>
                    </div>
                    <div class="text-2xl font-bold text-gray-900">
                        <%= kpis.proteines.valeurGrammes %>g / <%= kpis.proteines.objectifGrammes %>g
                    </div>
                    <div class="text-xs text-gray-500 mt-1"><%= kpis.proteines.pourcentage %>% de l'objectif</div>
                    <div class="w-full h-2 bg-gray-200 rounded mt-3">
                        <% var proteinesWidth = Math.min(kpis.proteines.pourcentage, 100); %>
                        <div class="h-2 bg-green-500 rounded" data-width="<%= proteinesWidth %>"></div>
                    </div>
                </div>

                <!-- Carte Glucides -->
                <div class="bg-white rounded-[16px] border border-[#E2E8F0] p-4">
                    <div class="flex items-center justify-between mb-2">
                        <span class="text-sm text-gray-600">Glucides</span>
                        <i class="fas fa-square text-red-500"></i>
                    </div>
                    <div class="text-2xl font-bold text-gray-900">
                        <%= kpis.glucides.valeurGrammes %>g / <%= kpis.glucides.objectifGrammes %>g
                    </div>
                    <div class="text-xs text-gray-500 mt-1"><%= kpis.glucides.pourcentage %>% de l'objectif</div>
                    <div class="w-full h-2 bg-gray-200 rounded mt-3">
                        <% var glucidesWidth = Math.min(kpis.glucides.pourcentage, 100); %>
                        <div class="h-2 bg-red-500 rounded" data-width="<%= glucidesWidth %>"></div>
                    </div>
                </div>
            </div>


            <!-- Grille principale (liste repas + sidebar) -->
            <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                <!-- Liste des repas -->
                <div class="lg:col-span-2 space-y-6">
                    <div class="bg-white rounded-lg shadow p-6">
                        <div class="flex items-center justify-between mb-4">
                            <div>
                                <h4 class="text-lg font-semibold text-gray-900">Repas d'aujourd'hui</h4>
                                <p class="text-xs text-gray-500">Analysés par l’IA</p>
                            </div>
                            <button type="button" class="text-xs text-gray-500 hover:text-gray-700">Voir tout</button>
                        </div>

                        <ul class="divide-y" id="liste-repas">
                            <% if (repasDuJour && repasDuJour.length > 0) { %>
                                <% repasDuJour.forEach(function(repas) { %>
                                    <li class="py-3 flex items-center justify-between">
                                        <div>
                                            <p class="text-sm font-medium text-gray-800"><%= repas.nom %></p>
                                            <p class="text-xs text-gray-500"><%= repas.heureFormatee %> • <%= repas.typeRepasFormate %></p>
                                        </div>
                                        <span class="text-xs bg-gray-100 text-gray-700 px-2 py-1 rounded"><%= repas.calories %> kcal</span>
                                    </li>
                                <% }); %>
                            <% } else { %>
                                <li class="py-4 text-center text-gray-500">
                                    <i class="fas fa-utensils text-2xl mb-2"></i>
                                    <p class="text-sm">Aucun repas enregistré aujourd'hui</p>
                                </li>
                            <% } %>
                        </ul>

                        <div class="mt-4">
                            <button type="button" class="text-sm bg-gray-100 hover:bg-gray-200 text-gray-700 px-4 py-2 rounded">
                                Ajouter un repas
                            </button>
                        </div>
                    </div>

                    <!-- Progression hebdomadaire -->
                    <div class="bg-white rounded-lg shadow p-6">
                        <div class="flex items-center justify-between mb-4">
                            <h4 class="text-lg font-semibold text-gray-900">Progression cette semaine</h4>
                            <button type="button" class="text-xs text-gray-500 hover:text-gray-700">Rapports</button>
                        </div>
                        <div class="h-40 bg-gray-50 border border-dashed border-gray-200 rounded flex items-center justify-center text-xs text-gray-400">
                            Graphique de progression interactif
                        </div>
                    </div>
                </div>

                <!-- Sidebar -->
                <div class="space-y-6">
                    <div class="bg-white rounded-lg shadow p-6">
                        <h4 class="text-lg font-semibold text-gray-900 mb-4">Actions rapides</h4>
                        <div class="space-y-3">
                            <a href="/analyser" class="w-full flex items-center justify-center bg-nutri-green text-white py-2 rounded hover:bg-emerald-600">
                                <i class="fas fa-camera mr-2"></i>
                                Analyser un repas
                            </a>
                            <button type="button" class="w-full flex items-center justify-center border py-2 rounded hover:bg-gray-50">
                                <i class="fas fa-sliders mr-2"></i>
                                Modifier objectifs
                            </button>
                            <button type="button" class="w-full flex items-center justify-center border py-2 rounded hover:bg-gray-50">
                                <i class="fas fa-chart-line mr-2"></i>
                                Voir rapports
                            </button>
                        </div>
                    </div>

                    <div class="bg-emerald-50 border border-emerald-200 rounded-lg p-4">
                        <div class="flex items-start">
                            <i class="<%= conseilDuJour.icone || 'fas fa-lightbulb' %> text-emerald-600 mr-2 mt-0.5"></i>
                            <div>
                                <p class="text-sm font-medium text-emerald-800"><%= conseilDuJour.titre %></p>
                                <p class="text-xs text-emerald-700"><%= conseilDuJour.contenu %></p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Pied de page -->
            <div class="mt-8 text-center">
                <p class="text-xs text-gray-500">NutriTrack • Assistant nutritionnel personnalisé</p>
            </div>
        </div>
    </div>
